export const metadata = {
  title: "搜尋作品 | Banngumi View | Annict.com",
};

const SearchPageLayout = async ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};
export default SearchPageLayout;
