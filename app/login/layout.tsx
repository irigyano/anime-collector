import { ReactNode } from "react";

export const metadata = {
  title: "登入 | Banngumi View | Annict.com",
  description: "輕鬆整理動畫清單",
};

const LoginLayout = ({ children }: { children: ReactNode }) => {
  return <>{children}</>;
};
export default LoginLayout;
