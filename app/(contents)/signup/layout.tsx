import { ReactNode } from "react";

export const metadata = {
  title: "註冊 | Banngumi View | Annict.com",
  description: "輕鬆整理動畫清單",
};

const SignUpLayout = ({ children }: { children: ReactNode }) => {
  return <>{children}</>;
};
export default SignUpLayout;
